# Whisper large-v3-turbo via faster-whisper (CTranslate2) — clean, fast, accurate
# word-level timestamps on CPU. faster-whisper pulls ctranslate2 + av (bundled ffmpeg).
faster-whisper>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
